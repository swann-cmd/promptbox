# 🧪 用户档案系统快速测试指南

## 浏览器访问
你的生产环境 URL：[请填写你的 Vercel 部署地址]

## 测试步骤

### 1️⃣ 基础功能测试（5分钟）

#### 测试 1.1：注册新用户自动创建档案
- [ ] 退出当前登录（如果已登录）
- [ ] 使用新邮箱注册一个账号
- [ ] 注册完成后，点击右上角头像
- [ ] 能否打开"编辑个人资料"弹窗？
- [ ] 显示的昵称是否默认为邮箱前缀？

#### 测试 1.2：编辑个人资料
- [ ] 点击右上角头像区域
- [ ] 弹窗是否正常打开？
- [ ] 修改昵称为：测试用户
- [ ] 修改简介为：这是我的简介
- [ ] 点击"保存更改"
- [ ] 页面顶部是否立即显示新的昵称？

#### 测试 1.3：社区显示优化
- [ ] 点击"社区"按钮进入社区广场
- [ ] 提示词卡片是否显示作者名称（不是"匿名用户"）？
- [ ] 是否显示用户头像（圆形图标）？

### 2️⃣ 用户主页功能（3分钟）

#### 测试 2.1：访问用户主页
- [ ] 在社区广场，点击任意提示词的作者名称
- [ ] 是否打开用户主页？
- [ ] 页面宽度是否与首页一致（max-w-5xl）？
- [ ] 顶部是否有返回按钮？

#### 测试 2.2：查看用户信息
- [ ] 是否显示用户头像（大尺寸）？
- [ ] 是否显示用户昵称？
- [ ] 是否显示个人简介（如果有）？
- [ ] 是否显示统计数据：发布数、获赞数、复制数、浏览数？

#### 测试 2.3：查看用户的提示词
- [ ] 是否显示"发布的提示词"标题？
- [ ] 是否显示该用户发布的所有提示词？
- [ ] 提示词卡片样式是否与社区一致？
- [ ] 点击提示词卡片能否打开详情？

### 3️⃣ 边界情况测试（2分钟）

#### 测试 3.1：没有设置昵称
- [ ] 注册时不填写姓名字段
- [ ] 社区中显示的名称是否为邮箱前缀？
- [ ] 用户主页显示的名称是否正确？

#### 测试 3.2：没有设置头像
- [ ] 不填写头像链接
- [ ] 是否显示默认的蓝色头像图标？
- [ ] 图标中间是否显示首字母？

#### 测试 3.3：输入验证
- [ ] 尝试输入超过 50 字符的昵称 → 应该显示限制
- [ ] 尝试输入超过 500 字符的简介 → 应该显示限制
- [ ] 输入无效的 URL 作为头像 → 应该显示错误提示

### 4️⃣ 数据库验证（可选）

在 Supabase SQL Editor 中执行：

```sql
-- 查看用户档案数量
SELECT COUNT(*) as 总用户数 FROM user_profiles;

-- 查看已设置昵称的用户
SELECT COUNT(*) as 已设置昵称 FROM user_profiles WHERE display_name IS NOT NULL;

-- 查看最近的档案
SELECT user_id, display_name, avatar_url, created_at 
FROM user_profiles 
ORDER BY created_at DESC 
LIMIT 5;
```

## ✅ 预期结果

所有测试都应该通过！如果有任何问题，请记录：
- 错误现象
- 错误信息（浏览器控制台）
- 重现步骤

## 🐛 常见问题

### 问题 1：社区还是显示"匿名用户"
**解决**：执行以下 SQL 为现有用户补充档案
```sql
-- 已在迁移脚本中自动执行，检查 user_profiles 表
SELECT COUNT(*) FROM user_profiles;
```

### 问题 2：点击用户名称报错
**解决**：确认已执行完整的迁移脚本，特别是修复 SQL 歧义的部分

### 问题 3：样式不一致
**解决**：强制刷新浏览器（Cmd+Shift+R）

## 🎉 测试完成

如果所有测试通过，恭喜！用户档案系统已成功上线！

---
生成时间：2026-03-12
